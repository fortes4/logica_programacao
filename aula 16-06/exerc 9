/*9) Faça um programa que leia um número n e imprima se ele e´ primo ou não. (um número primo tem apenas 2 divisores: 1 e ele mesmo! O número 1 não é primo!!!)*/

#include <stdio.h>
#include <conio.h>

int main(){
 int num, i, resultado = 0;
 
 printf("Digite um numero: ");
 scanf("%d", &num);
 
 for (i = 2; i <= num / 2; i++) {
    if (num % i == 0) {
       resultado++;
       break;
    }
 }
 
 if (resultado == 0)
    printf("%d e um numero primo!!\n", num);
 else
    printf("%d nao e um numero primo!!\n", num);
 
 return 0;
}
